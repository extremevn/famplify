name: flutter_amplify_sdk
description: Flutter plugin aws cognito and api
version: 0.0.1
homepage: https://github.com/extremevn/famplify

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: vn.com.extremevn.flutter_amplify_sdk
        pluginClass: FlutterAmplifySdkPlugin
      ios:
        pluginClass: FlutterAmplifySdkPlugin